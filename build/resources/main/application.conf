ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.petrescue.ApplicationKt.module ]
    }
}

app {
    storage {
        type = "local"
        localPath = "uploads"
    }
    database {
        url = "jdbc:h2:file:./data/petrescue;MODE=PostgreSQL;AUTO_SERVER=TRUE"
        driver = "org.h2.Driver"
    }
}
